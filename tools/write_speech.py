"""
批量写入 EdgeGuard 汇报答辩讲稿到 DOCX 文件。
通过 edsdk.py 调用本地 editor_sdk。
"""

import subprocess
import json
import os

EDSDK = r"D:\Workbuddy\resources\app.asar.unpacked\resources\builtin-skills\tencent-local-office-edit\edsdk.py"
FILE_ID = "new_doc_6081433572200_b54a"
PYTHON = "python3"

def call_edsdk(tool, **kwargs):
    """调用 edsdk.py"""
    args = [PYTHON, EDSDK, "call", tool]
    for k, v in kwargs.items():
        if isinstance(v, str) and not v.startswith("{") and not v.startswith("["):
            args.append(f"{k}={v}")
        else:
            args.extend(["--json", json.dumps({k: v})])
    result = subprocess.run(args, capture_output=True, text=True, timeout=30, cwd=r"C:\Users\SQS\Desktop\edgeguard")
    if result.returncode != 0:
        print(f"ERROR ({tool}): {result.stderr}")
        return None
    print(f"OK: {tool}")
    return result.stdout

# 使用 doc_insert_html_content 一次性写入所有内容
sections = []

# ===== 第二部分：系统总体设计 =====
sections.append("""
<h2>三、系统总体设计（对应 Slide 6-8）</h2>

<h3>3.1 系统部署与工程实现（Slide 6）</h3>

<p>下面来看我们的系统规模。整个项目包含约3万行AI核心算法与工程代码，提供44个标准化RESTful API接口，建立了5个实时双向WebSocket长连接通道，支持8种多模态人机交互手势映射，内置37条复杂场景智能决策逻辑链，并开放了14个可扩展的Function Calling工具。这些数字体现了我们在工程实现上的完整度和深度。</p>

<p>在工程化部署方面，我们提供了两种方案。本地开发只需执行start.bat一键拉起，自动安装依赖，秒级启动开发环境。容器交付则通过Docker Compose编排，环境完全隔离，支持跨平台快速迁移。</p>

<p>在架构设计上，我们采用高内聚模块化设计。backend层基于FastAPI实现路由分发与鉴权；frontend层基于Vue3构建响应式交互界面；modules层封装了vision、audio、AI和sys四大核心能力模块；data层使用SQLite进行数据持久化。</p>

<h3>3.2 系统总体架构（Slide 7）</h3>

<p>我们的系统采用五层架构设计，自底向上分别是数据层、感知层、决策层、服务层和展示层。</p>

<p>数据层采用双SQLite数据库架构——edgeguard.db存储系统运行状态与设备日志，user_memory.db持久化用户交互特征与个性化记忆数据，实现轻量化本地管理。</p>

<p>感知层全部运行于边缘侧，基于MediaPipe实现面部468关键点与手势21点识别，通过TFLite部署；集成Whisper语音识别模型，实现多模态交互数据的毫秒级采集与预处理。</p>

<p>决策层是系统的核心大脑，内置37条本地规则引擎保障极速响应，融合LangGraph多智能体协同框架，通过EdgeCloudRouter实现边云任务动态路由，结合RAG与FAISS增强推理能力。</p>

<p>服务层基于FastAPI网关，提供44个标准化RESTful API接口，配置5个WebSocket长连接通道，实现服务端与前端毫秒级状态推送。</p>

<p>展示层采用Vue3 SPA交互端，集成Dashboard与Report可视化视图，部署了14个定制化HMI交互面板，搭载edge_tts语音合成引擎实现低延迟语音反馈交互闭环。</p>

<p>总结来说，感知层全部运行于边缘侧保障50毫秒实时性；决策层采用本地规则与云端LLM双引擎；展示层通过WebSocket实现毫秒级状态推送。这就是我们的五层协同架构。</p>

<h3>3.3 技术选型（Slide 8）</h3>

<p>在技术选型上，我们做了充分的技术对比与论证。</p>

<p>后端框架我们选择FastAPI而非Flask，因为FastAPI原生支持async/await协程并发，完美支撑44个REST接口与WebSocket实时通信，并且能自动生成标准OpenAPI文档，大幅降低联调成本。</p>

<p>大模型方面，我们构建了多模型路由机制，兼容OpenAI接口标准。deepseek-reasoner处理复杂推理任务，包括诊断、分析和行程规划；豆包Lite通过Ark API处理快速任务，包括安全校验、意图识别和人机交互。按Agent角色自动路由分派，实现了性能与成本的最优平衡。</p>

<p>编排引擎我们选择LangGraph而非传统脚本编排，基于StateGraph状态图设计，天然适配多Agent协作流程，支持人工干预节点插入，灵活应对复杂决策逻辑与任务拆解。</p>

<p>计算机视觉方面，我们选择MediaPipe替代dlib，解决了dlib在Python 3.11环境下的兼容性问题。Google官方维护，CPU端推理速度小于15毫秒每帧，轻量化且功能覆盖全面。</p>

<p>向量检索我们使用FAISS而非Milvus或Chroma。Meta出品，针对小规模场景——384维向量——零依赖部署，IndexFlatIP内积检索极致高效，满足边缘端低延迟需求。</p>

<p>数据库方面我们选择SQLite而非MySQL，边缘端零部署、零配置，单文件存储便于备份，实现系统配置数据与Agent记忆数据的物理隔离，提升数据安全性。</p>
""")

# ===== 第三部分：关键技术实现 =====
sections.append("""
<h2>四、关键技术实现——多模态感知（对应 Slide 9-12）</h2>

<h3>4.1 面部疲劳特征提取（Slide 9）</h3>

<p>这是我们的第一个核心技术模块。我们依托MediaPipe Face Landmarker模型，精准捕捉468个3D面部特征点及虹膜位置，构建多维度的面部生物特征数据基础，为后续疲劳状态的精准识别提供核心支撑。</p>

<p>具体来说，我们实现了三个关键指标：</p>

<p>第一，眼纵横比，也就是EAR指标。通过眼部特征点计算纵向与横向距离比值，设定阈值0.22为判定界限。当EAR数值低于该阈值时，即判定为眼睛闭合，可有效捕捉瞬目频率、闭眼时长等微疲劳关键特征。</p>

<p>第二，PERCLOS指标，这是国际通用的疲劳判定金标准。计算单位时间内眼睛闭合的累计时长占比，是目前国际公认的疲劳驾驶检测核心指标。通过统计连续时段内的闭眼占比，可量化评估驾驶员的嗜睡程度与注意力衰减风险。</p>

<p>第三，头部姿态解算。利用3D到2D特征点映射，通过solvePnP算法解算Pitch俯仰角和Yaw偏航角。当偏航角超过正负10度或俯仰角超过正负8度时，系统即刻判定为驾驶员注意力分散或低头行为。</p>

<p>这三大指标相互补充，共同构成了我们的面部疲劳特征提取体系。从单一EAR到PERCLOS再到PnP三维姿态，实现了从"眨眼检测"到"注意力全面监测"的跨越。</p>

<h3>4.2 本地决策引擎——三级疲劳分级（Slide 10）</h3>

<p>在特征提取之后，我们构建了本地决策引擎，核心是三级疲劳分级机制。</p>

<p>核心逻辑是：融合闭眼时长、眨眼频率、PERCLOS、打哈欠强度及头部姿态特征，经过37条专家规则加权计算，输出0到100的疲劳指数。</p>

<p>根据疲劳指数，我们设置了三级递进式响应：</p>

<p>第一级，轻度提醒。疲劳评分在30到50之间，出现初期疲劳征兆时，系统发出柔和TTS语音提示，比如"请保持专注"，仅作温和提醒，不干扰驾驶操作。</p>

<p>第二级，中度告警。评分在50到75之间，疲劳状态持续恶化时，触发高频语音警告，中控屏橙色醒目闪烁，主动强化干预以唤醒驾驶员注意力。</p>

<p>第三级，危险强干预。评分超过75，极度疲劳或闭眼驾驶时，触发尖锐声光强制警报，系统自动记录事件日志，强烈建议立即停车休息。</p>

<p>这个决策引擎有三大特点：精准行为判定——打哈欠检测融合"嘴部开合度"与"持续帧数"双重校验，有效过滤喝水、说话等误触发场景；极致推理性能——CPU端单帧处理耗时小于15毫秒，实时推理帧率稳定25到30FPS；边缘自主决策——全流程在终端侧独立完成，无需云端交互，不依赖网络信号，确保在隧道、山区等无网环境下依然稳定运行。</p>

<h3>4.3 手势识别——无接触式人车交互（Slide 11）</h3>

<p>第二个感知通道是手势识别。我们采用双引擎协同识别架构：MediaPipe实时捕捉手部21个关键坐标点，结合轻量化TFLite自定义分类器进行端侧精准解析，构建毫秒级响应的识别底座。</p>

<p>性能方面，单帧分类推理耗时小于10毫秒，模型计算仅需1毫秒，实现驾驶场景下的即时响应，彻底解放驾驶员双手，杜绝因触控操作导致的视线偏离与分心风险。</p>

<p>我们定义了8种核心手势指令映射：张开手掌对应开启空调，紧握拳头对应关闭空调，竖大拇指对应确认指令，竖小拇指对应取消指令，OK手势也对应确认指令，Peace手势对应取消指令，食指指向用于注意力提示，Quiet Coyote手势用于一键静音。这8种手势覆盖了驾驶场景中最常用的控制操作。</p>

<h3>4.4 语音交互全链路（Slide 12）</h3>

<p>第三个感知通道是语音。我们构建了从采集到合成的完整语音交互全链路。</p>

<p>第一步，语音采集。基于recorder.py实现端侧音频捕获，配合降噪算法，保障嘈杂车规环境下的语音数据高保真。</p>

<p>第二步，智能识别。集成Whisper多语言模型，支持20多种语种实时转写，端侧推理响应小于2秒。</p>

<p>第三步，意图理解。DeepSeek LLM深度语义解析，精准提取用户意图槽位与action_code，实现复杂指令的准确理解。</p>

<p>第四步，智能执行。任务调度Agent协同多域执行，覆盖导航规划、多媒体控制及整车系统的智能联动响应。</p>

<p>第五步，语音合成。edge-tts引擎提供多音色拟人化播报，支持情感语音调节，实现自然流畅的人机语音交互体验。</p>

<p>这套语音系统有两个关键设计亮点：一是高可扩展的三段式解耦架构——识别、理解、合成三大核心模块完全解耦，支持独立升级与算法热替换，可灵活适配端侧轻量模型与云端大模型的混合部署。二是疲劳监测与告警的闭环联动——复用语音交互全链路，将视觉监测的疲劳状态实时转化为多音色语音提醒，形成"监测、分析、告警"的完整闭环，拟人化播报增强警示效果。</p>
""")

# ===== 第四部分：核心创新 =====
sections.append("""
<h2>五、核心创新——多Agent与边云协同（对应 Slide 13-17）</h2>

<h3>5.1 LangGraph多智能体协同架构——快速层（Slide 13）</h3>

<p>这是我们的第一个核心创新，也是整个系统最亮眼的部分。我们基于LangGraph构建了多智能体协同架构，分为快速层和推理层两个层次。</p>

<p>先看快速层的处理流程。当用户通过语音、手势或车辆状态发起输入后，首先由Orchestrator进行任务编排与Pydantic Schema校验，然后进入Safety Agent安全裁决。Safety Agent拥有VETO否决权，如果判定指令威胁驾驶安全，直接拦截，不进入后续流程。</p>

<p>通过安全检查后，Intention Agent进行意图分解与路由调度，将模糊的自然语言转化为结构化、可执行的任务序列，路由到六个Agent中匹配的子集执行。Interaction Agent负责指令执行与行为控制。六个Agent按角色分派不同模型：快速任务走豆包Lite，复杂推理走deepseek-reasoner。最后，所有输出必须通过Safety Gate的多重校验，解决指令冲突与误判，确保每一条下发的指令都是绝对安全的。</p>

<p>快速层的设计精髓在于四个字：安全优先。从Safety Agent的VETO前置，到Safety Gate的闭环复核，安全始终是第一优先级。</p>

<h3>5.2 LangGraph多智能体协同架构——推理层（Slide 14）</h3>

<p>推理层承担更复杂的认知任务，包含三个Agent，全部使用deepseek-reasoner推理模型。</p>

<p>Diagnose Agent负责RAG诊断与故障分析。通过FAISS向量检索从知识库中提取故障码、法规条文与维修指引，结合deepseek-reasoner的推理能力实现精准诊断。</p>

<p>Analyze Agent负责行为分析与驾驶报告。基于PERCLOS、EAR、PnP三维指标与驾驶行为数据，生成结构化驾驶报告与个性化疲劳基线模型。</p>

<p>Recommend Agent负责行程规划与POI推荐。结合实时环境信息，包括天气、路况、服务区数据，与行程模板库，为每位驾驶员提供个性化行程规划与POI推荐，实现千人千面。</p>

<p>同样，推理层的输出也通过Safety Gate加Pydantic Schema多重校验，确保诊断、分析与推荐结果的准确性和安全性。</p>

<h3>5.3 SafetyAgent四级状态机与VETO机制（Slide 15）</h3>

<p>接下来我想重点展开SafetyAgent的设计，这是整个系统的安全基石。</p>

<p>我们设计了一个四级递进式风险判定模型：Normal正常驾驶、Attention注意力下降、Distracted视线偏移超过10度、Dangerous高风险告警。四个级别逐级递进，越高级别触发越强的干预策略。</p>

<p>在系统级可靠性方面，我们实现了三层兜底保护。第一层，超时熔断保护——Agent调用超过8秒自动终止，避免阻塞主流程。第二层，模型加载优雅降级——若感知模型加载失败，自动回退至基础规则引擎，确保核心监测功能不中断。第三层，异常沙箱隔离——单Agent模块异常时通过进程隔离防止错误扩散。</p>

<p>最核心的是VETO否决机制。我举一个典型冲突场景：当驾驶员被判定为危险疲劳状态时，如果发出指令"播放摇滚并音量最大"，此时娱乐Agent的执行计划将被SafetyAgent拦截，触发最高优先级的安全否决。系统不仅拒绝执行，还会主动建议"您已疲劳，请靠边休息"，并自动唤起导航至最近服务区。这就是"安全优先于一切"的设计理念。</p>

<h3>5.4 边云协同路由——EdgeCloudRouter（Slide 16）</h3>

<p>第二个核心创新是边云协同路由。我们设计了一个智能路由分发机制，将用户请求分为三条路径。</p>

<p>第一条，本地规则引擎路径。处理简单指令、安全告警及基础控制，内置37条核心规则，实现小于50毫秒超低延迟响应。断网状态下依然稳定运行，筑牢车辆安全底线。</p>

<p>第二条，多模型云端路径。快速任务走豆包Lite，响应小于1秒；复杂推理走deepseek-reasoner，响应2到6秒。按Agent角色自动路由分派，实现成本与延迟最优平衡。</p>

<p>第三条，混合路径，结合本地预处理与云端增强，实现弹性降级。</p>

<p>这个设计带来了三个关键收益：安全永不失联——将疲劳监测、紧急告警与基础车辆控制完全边缘化，彻底摆脱网络依赖；智能按需上云——简单高频任务本地消化，复杂认知任务云端赋能，有效平衡算力消耗与智能体验；隐私极致保护——驾驶影像与车内敏感数据严格保留在车端，仅将脱敏后的文本化用户意图加密上云，从数据源头守护用户隐私安全。</p>

<h3>5.5 RAG知识增强与Agent记忆系统（Slide 17）</h3>

<p>第三个核心创新是RAG知识增强加Agent记忆加证据溯源三位一体的知识体系。</p>

<p>在RAG车载知识库方面，我们构建了完整的知识库引擎。原始文档包括vehicle_manual.txt和safety_guidelines.txt，以及trip_templates.json行程模板。经过文本切块、384维向量嵌入、FAISS索引构建，形成了高效的车载知识检索体系。当用户自然语言提问时，系统执行语义检索取Top-3相似文本，动态拼接上下文，由DeepSeek LLM生成专业、精准的回答。实测专业知识问答准确率超过80%。特别值得一提的是，我们实现了证据溯源机制，以KB:manual §X.Y格式自动标注引用来源，聚合节点去重编号生成参考列表。</p>

<p>在Agent记忆方面，我们设计了双层记忆系统。工作记忆依托内存实时缓存N轮对话历史，确保多轮交互的逻辑连贯性与语境一致性。长期记忆通过SQLite数据库持久化存储用户偏好，比如空调习惯22度、音乐口味等，实现跨会话的记忆留存，打造真正懂你的个性化车载智能体。</p>
""")

# ===== 第五部分：系统实现与演示 =====
sections.append("""
<h2>六、系统实现与演示验证（对应 Slide 18-20）</h2>

<h3>6.1 前端HMI实现（Slide 18）</h3>

<p>在展示层，我们基于Vue 3.4加Vite 5构建极速开发体验，结合Tailwind CSS与Element Plus打造灵活界面，利用ECharts与Leaflet实现数据可视化与地图集成。</p>

<p>我们实现了五个核心交互面板：</p>

<p>GaugePanel状态仪表，基于ECharts渲染驾驶员疲劳评分0到100与PERCLOS指标，以动态仪表盘形式实时反馈生理状态数据。</p>

<p>AlertPanel智能分级告警，实现Mild、Moderate、Dangerous三级视觉警示，通过颜色动态变化与弹窗提醒，确保风险信息即时触达。</p>

<p>多模态交互面板，支持手势识别实时反馈，打通"语音输入到AI意图理解到TTS语音合成"的全链路自然交互体验。</p>

<p>AgentResultPanel决策可视化，直观呈现多智能体的协同执行过程与决策逻辑，实现复杂任务调度的流式可视化。</p>

<p>智能座舱与车控HMI，集成音乐播放、自动空调16到30度智能恒温、Leaflet高精度地图导航，打造沉浸式车载交互体验。</p>

<p>底层通信方面，我们建立了5路高并发WebSocket连接，实现端到端数据推送延迟小于100毫秒，保障交互操作零卡顿。</p>

<h3>6.2 API设计：44 REST加统一规范（Slide 19）</h3>

<p>在API层面，我们设计了44个标准化RESTful接口，按功能分为8个分组。健康状态类包括health和analyze端点；Agent编排类包括orchestrate、thinking和chat端点；环境感知类包括environment、weather和navigation端点；智能车控类包括空调命令、音乐播放等端点；语音交互类包括语音转写、TTS合成等端点；知识库类提供prompts模板管理；数据服务类提供session、alerts和interactions查询；配置管理类提供settings和dashboard状态接口。</p>

<p>所有接口返回标准JSON格式，错误信息包含错误码与详细描述，便于前端统一处理。</p>

<h3>6.3 演示环节设计（Slide 20）</h3>

<p>我们设计了五个演示环节来系统验证各项核心能力：</p>

<p>演示一，实时监测。正对摄像头保持自然坐姿，展示面部468特征点网格渲染、EAR眨眼曲线实时波动、仪表盘式疲劳评分动态跳动。核心验证点是端侧推理实时性达到25到30FPS，以及算法轻量化与硬件算力的高效协同。</p>

<p>演示二，疲劳告警升级。通过闭眼3秒、假装连续打哈欠、低头看手机等行为，展示告警面板颜色由黄到橙到红渐变、TTS语音警告逐级增强、中控屏弹出强制休息建议。核心验证点是三级状态机与多特征融合，以及多模态数据交叉验证降低误报率。</p>

<p>演示三，多Agent协同。发出语音指令"我有点累，帮我找个地方休息"，展示意图识别、安全校验、周边检索、导航规划的全流程可视化，以及多个智能体状态实时更新。核心验证点是智能体协作与VETO机制，以及复杂场景下的自主决策与安全兜底。</p>

<p>演示四，无接触手势控制。隔空张掌开空调、OK手势确认、左右挥手切歌调温，展示手势骨架实时识别并高亮、空调和音乐面板随动响应、指令执行无明显延迟。核心验证点是毫秒级端侧交互小于10毫秒，以及视线不离路面的安全交互体验。</p>

<p>演示五，断网降级——这是我们的杀手锏。现场物理拔断网络线缆，再次模拟疲劳动作，展示本地告警逻辑照常触发、基础车控功能无影响、中控提示"已切换至本地应急模式"。核心验证点是边云协同的高可用性——断网不停机，核心安全功能不降级。</p>
""")

# ===== 第六部分：总结 =====
sections.append("""
<h2>七、创新点总结与展望（对应 Slide 21-22）</h2>

<h3>7.1 四大创新点总结（Slide 21）</h3>

<p>最后，我将我们的核心创新总结为四点：</p>

<p>第一，多模态边缘感知融合。融合面部EAR、PnP、PERCLOS、手势与语音三通道特征，通过多维度加权算法显著降低误报率。全流程边缘侧处理时延低于50毫秒，保障毫秒级实时响应。</p>

<p>第二，安全优先的多Agent架构。首创车载场景SafetyAgent VETO一票否决机制，六个Agent按角色分派豆包Lite快速模型与deepseek-reasoner复杂推理模型，Pydantic Schema校验输出，确保安全决策优先级绝对凌驾于用户指令之上。</p>

<p>第三，边云协同弹性架构。本地37条核心规则引擎与多模型云端三路径智能路由，断网时自动无感降级为纯本地模式，安全功能永在线。</p>

<p>第四，RAG加记忆加证据溯源三增强。基于FAISS构建车载专业知识库，知识命中率超80%，KB:manual §X.Y格式证据溯源自动标注引用来源，结合长短时双层记忆持续学习用户习惯，实现"懂车更懂你"的个性化交互。</p>

<p>如果用一句话总结：EdgeGuard的本质，是把"监测、决策、干预、服务"闭环第一次完整地搬进了边缘车载终端。</p>

<h3>7.2 实训总结与未来展望（Slide 22）</h3>

<p>回顾这四周的实训过程，我们成功构建了从边缘感知、本地决策、多Agent协同到前端HMI交互的全栈式车载智能系统闭环。系统集成44个核心API接口与5路高并发WebSocket通信链路，采用双数据库协同架构。经实测，系统响应延迟、数据吞吐量及决策准确率等关键指标均100%达成预设目标，具备极高的稳定性与工程落地价值。成果交付方面，系统已完成全功能联调，可无缝衔接车规级量产需求。</p>

<p>展望未来，我们有三个方向可以持续推进：</p>

<p>第一，模型轻量化与端侧部署。引入Whisper-turbo及知识蒸馏技术，将大模型体积大幅压缩，适配车规级低功耗芯片，在保证识别精度的同时实现毫秒级端侧推理。</p>

<p>第二，实车验证与总线打通。突破仿真环境限制，实现与真实车辆CAN/LIN总线的物理层对接，解析车速、转向等核心信号，完成从虚拟演示到实车功能部署的跨越。</p>

<p>第三，个性化画像与主动服务。基于长期驾驶行为数据，构建动态驾驶员身份识别与疲劳基线模型，实现自适应座舱交互策略与千人千面的主动安全预警服务。</p>

<h2>八、结束语（对应 Slide 23）</h2>

<p>以上就是我们第五小组的全部汇报内容。EdgeGuard——让你的每一次出行都有AI守护。恳请各位老师批评指正，谢谢！</p>
""")

# 写入所有内容
idx = 67  # 从上一个插入位置开始
for i, section in enumerate(sections):
    payload = {"html_text": section.strip()}
    print(f"\n=== Writing section {i+1}/{len(sections)} ===")
    args = [PYTHON, EDSDK, "call", "doc_insert_html_content",
            f"file_id={FILE_ID}", f"idx={idx}",
            "--json", json.dumps(payload)]
    result = subprocess.run(args, capture_output=True, text=True, timeout=30,
                          cwd=r"C:\Users\SQS\Desktop\edgeguard")
    if result.returncode != 0:
        print(f"ERROR: {result.stderr}")
        break
    print(f"OK (section {i+1})")
    # 解析新的 idx
    try:
        resp = json.loads(result.stdout)
        idx = resp.get("last_edit_index", idx + len(section.strip()))
    except:
        idx += len(section.strip())

print("\n=== All sections written! ===")
