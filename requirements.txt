# EdgeGuard — 车载多模态AI系统
# 安装: pip install -r requirements.txt
#
# 外部服务依赖（非 Python）：
#   网易云音乐 API → cd tools/netease-cloud-music-api && npm install && node app.js
#   （默认监听 localhost:3000，歌曲搜索/播放必需）
#   PyExecJS → 小红书签名引擎运行 JS 所需（可选，缺失则 XHS 降级）
PyExecJS>=1.5.1

# 视觉 & 感知
opencv-python>=4.8.0
mediapipe>=0.10.30
numpy>=1.24.0

# 手势识别 (MediaPipe Tasks API + TFLite 分类器)
tensorflow>=2.15.0

# 语音
sounddevice>=0.5.2
webrtcvad>=2.0.7
openai-whisper>=20240930
noisereduce>=3.0.0
edge-tts>=6.1.0

# LLM & AI
openai>=1.30.0
langchain>=0.3.0
langgraph>=0.2.0

# 向量库 & Embeddings
faiss-cpu>=1.10.0
sentence-transformers>=3.0.0

# 后端
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
httpx>=0.28.0

# 工具
python-dotenv>=1.0.0
requests>=2.31.0
